/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { SearchByNumeroPartePipe } from './search-by-numero-parte.pipe';

describe('Pipe: SearchByNumeroPartee', () => {
  it('create an instance', () => {
    let pipe = new SearchByNumeroPartePipe();
    expect(pipe).toBeTruthy();
  });
});
