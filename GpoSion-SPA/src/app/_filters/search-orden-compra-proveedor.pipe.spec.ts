/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { SearchOrdenCompraProveedorPipe } from './search-orden-compra-proveedor.pipe';

describe('Pipe: SearchOrdenCompraProveedore', () => {
  it('create an instance', () => {
    let pipe = new SearchOrdenCompraProveedorPipe();
    expect(pipe).toBeTruthy();
  });
});
