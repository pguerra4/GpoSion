/* tslint:disable:no-unused-variable */

import { TestBed, waitForAsync } from '@angular/core/testing';
import { SearchMaterialExistenciasPipe } from './search-material-existencias.pipe';

describe('Pipe: SearchMaterialExistenciase', () => {
  it('create an instance', () => {
    let pipe = new SearchMaterialExistenciasPipe();
    expect(pipe).toBeTruthy();
  });
});
