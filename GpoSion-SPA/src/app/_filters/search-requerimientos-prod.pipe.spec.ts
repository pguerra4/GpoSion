/* tslint:disable:no-unused-variable */

import { TestBed, waitForAsync } from '@angular/core/testing';
import { SearchRequerimientosProdPipe } from './search-requerimientos-prod.pipe';

describe('Pipe: SearchRequerimientosProde', () => {
  it('create an instance', () => {
    let pipe = new SearchRequerimientosProdPipe();
    expect(pipe).toBeTruthy();
  });
});
