export interface LocalidadNumeroParte {
  localidadId: number;
  localidad: string;
  noParte: string;
  existencia: number;
  fechaCreacion?: Date;
  ultimaModificacion?: Date;
  creadoPor?: string;
  modificadoPor?: string;
}
